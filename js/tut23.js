console.log('Maths onject');
let x=4;
let y=2;
let z;
z=x+y;
z=x-y;
z=x*y;
z=x/y;
z=x%y;
z=x||y;

// using math object
z=Math;
z=Math.PI;
z=Math.round(2.2);
z=Math.ceil(2.2);
z=Math.abs(5.5);
z=100*Math.random();  // it creatre random number between 0 to 100
z=25+(100-25)*Math.random(); // it create random number between 25 to 100
z=Math.ceil(25+(100-25)*Math.random()); //after using ceil function it gives only integer value

console.log(z);